{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "format": "prettier --write 'src/**/*.{svelte,js,css}'",
    "lint": "prettier --check 'src/**/*.{svelte,js,css}'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "prettier": "^3.8.4",
    "prettier-plugin-svelte": "^4.1.0",
    "svelte": "^5.55.5",
    "vite": "^8.0.12",
    "vitest": "^4.1.8"
  }
}
